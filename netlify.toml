[build]
  command = "npm run build"
  # Plugin will populate .netlify/output/public (created by postbuild script)
  publish = ".netlify/output/public"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

